
import { Drug, TrackingEvent, DrugStatus } from '../services/types';

// Import and re-export the mock data
export { mockDrugs, mockEvents, computeDrugStatus, filterEventsByRole } from '../../../supabase/functions/fabric-chaincode/mockData.ts';
